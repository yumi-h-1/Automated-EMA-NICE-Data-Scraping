# Copy to .env and fill in, or export these before running.
# Only needed for the indication columns; --skip-llm works without a key.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Optional: point at a specific Python interpreter for the analysis half.
# EMA_NICE_PYTHON=/usr/local/bin/python3
