"""Enrichment and quality checks for pages collected by the Node crawler.

The crawler produces a `crawl.json` of trimmed markup; everything in this
package turns that into the published dataset and then checks it.
"""

__version__ = '0.1.0'
